# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["...", "chromium", "google-chrome"]

[phases.install]
cmds = [
    "pip install -r backend/requirements.txt",
    "playwright install chromium",
    "playwright install-deps chromium"
]

[phases.build]
cmds = [
    "echo '✅ Chromium instalado para Playwright'",
    "echo '⚠️ Undetected Chrome requiere Google Chrome que no está disponible en Railway'",
    "echo '📌 Usando Playwright con stealth mode como alternativa'"
]

[start]
cmd = "cd backend && uvicorn api.server:app --host 0.0.0.0 --port $PORT"
